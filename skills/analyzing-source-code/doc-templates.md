# Documentation Page Templates

These are the 4 documentation page templates generated by parallel writing agents. Each page follows a consistent structure in zh-TW.

## Template 1: architecture.md

```markdown
---
layout: doc
---

# {Project Name} — 系統架構

::: info 相關章節
- 各元件的核心邏輯請參閱 [核心功能分析](./core-features)
- 控制器架構與 CRD 定義請參閱 [控制器與 API](./controllers-api)
- 與外部系統的整合方式請參閱 [外部整合](./integration)
:::

## 概述

{1-2 paragraphs: what the project does, language, license, key stats}

| 項目 | 內容 |
|------|------|
| **GitHub** | [{org}/{repo}]({url}) |
| **語言** | {language} {version} |
| **授權** | {license} |
| **建置工具** | {build tools} |

## 系統架構圖

\```mermaid
graph TB
    subgraph "控制平面"
        A[Component A] --> B[Component B]
    end
    subgraph "資料平面"
        C[Component C]
    end
    A --> C
\```

## 核心元件 / Binary

| Binary | 路徑 | 功能 |
|--------|------|------|
| {name} | `cmd/{name}/` | {description} |

## 目錄結構

\```
project/
├── cmd/           # Binary 入口
├── pkg/           # 核心套件
├── api/           # CRD 型別定義
├── controllers/   # 控制器
├── config/        # K8s 部署設定
└── test/          # 測試
\```

## CRD 資源總覽

| CRD | API Group | 說明 |
|-----|-----------|------|
| {Name} | {group}/{version} | {description} |

## 建置系統

### Makefile 目標

| 目標 | 說明 |
|------|------|
| `make build` | {description} |
| `make test` | {description} |

### CI/CD

{Document GitHub Actions workflows}

## 狀態機 (if applicable)

\```mermaid
stateDiagram-v2
    [*] --> Pending
    Pending --> Running
    Running --> Succeeded
    Running --> Failed
\```
```

## Template 2: core-features.md

```markdown
---
layout: doc
---

# {Project Name} — 核心功能分析

::: info 相關章節
- 系統架構與元件說明請參閱 [系統架構](./architecture)
- 控制器如何觸發這些功能請參閱 [控制器與 API](./controllers-api)
- 與外部系統的整合請參閱 [外部整合](./integration)
:::

## 概述

{Brief overview of what features are covered}

## {Feature 1 Name}

### 運作機制

{Explanation with context}

\```go
// 檔案: pkg/path/to/file.go
func ImportantFunction() {
    // real code from the repository
}
\```

::: tip 重點
{Key insight about this feature}
:::

### 處理流程

\```mermaid
sequenceDiagram
    participant A as Component A
    participant B as Component B
    A->>B: Request
    B-->>A: Response
\```

## {Feature 2 Name}

### 實作細節

{Detailed analysis with code}

::: info 設計決策
{Why was this approach chosen?}
:::

## {Feature N Name}

{Continue pattern...}

## 小結

{Summary table of all features covered}

| 功能 | 實作位置 | 關鍵特性 |
|------|----------|----------|
| {name} | `pkg/path/` | {key characteristic} |
```

## Template 3: controllers-api.md

```markdown
---
layout: doc
---

# {Project Name} — 控制器與 API

::: info 相關章節
- 系統整體架構請參閱 [系統架構](./architecture)
- 各控制器觸發的實際處理邏輯請參閱 [核心功能分析](./core-features)
- 外部整合的認證與協調細節請參閱 [外部整合](./integration)
:::

## 概述

{Overview of controller architecture and API types}

## 控制器總覽

| 控制器 | 監聽資源 | 檔案 |
|--------|----------|------|
| {Name}Reconciler | {Resource} | `controllers/{file}.go` |

## {Primary Controller} 詳解

### Reconciler 結構

\```go
// 檔案: controllers/{name}_controller.go
type {Name}Reconciler struct {
    client.Client
    Scheme *runtime.Scheme
    // ... real fields
}
\```

### Reconcile 流程

\```mermaid
flowchart TD
    A[Fetch Resource] --> B{Exists?}
    B -->|No| C[Return]
    B -->|Yes| D{Deleted?}
    D -->|Yes| E[Cleanup]
    D -->|No| F[Reconcile Logic]
\```

### 關鍵程式碼

\```go
// 檔案: controllers/{name}_controller.go
func (r *{Name}Reconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
    // real reconcile code
}
\```

## CRD 型別定義

### {Resource} Spec

\```go
// 檔案: api/{version}/{name}_types.go
type {Resource}Spec struct {
    // real fields from source
}
\```

### {Resource} Status

\```go
// 檔案: api/{version}/{name}_types.go
type {Resource}Status struct {
    // real fields from source
}
\```

## Webhook 機制

### 驗證 Webhook

{Document validation rules from *_webhook.go}

::: warning 注意
{Important validation constraint}
:::

## RBAC 規則

| 資源 | 權限 |
|------|------|
| {resource} | {verbs} |
```

## Template 4: integration.md

```markdown
---
layout: doc
---

# {Project Name} — 外部整合

::: info 相關章節
- 系統架構與元件說明請參閱 [系統架構](./architecture)
- 整合過程中涉及的核心機制請參閱 [核心功能分析](./core-features)
- 控制器如何協調這些整合請參閱 [控制器與 API](./controllers-api)
:::

## 概述

{Overview of how this project integrates with the ecosystem}

## {Primary Integration} 整合

### 整合方式

{How the integration works}

\```go
// 檔案: pkg/path/to/integration.go
// real integration code
\```

### 整合架構

\```mermaid
graph LR
    A[This Project] -->|API calls| B[External Project]
    B -->|Events| A
\```

::: info 整合重點
{Key integration details}
:::

## {Secondary Integration} 整合

{Continue pattern...}

## 認證與授權 (if applicable)

### 認證機制

{JWT, mTLS, ServiceAccount, etc.}

### RBAC 模型

| ClusterRole | 用途 | 關鍵權限 |
|-------------|------|----------|
| {name} | {purpose} | {key permissions} |

## CI/CD 整合

### GitHub Actions

| Workflow | 觸發條件 | 功能 |
|----------|----------|------|
| {name}.yaml | {trigger} | {description} |

## 使用範例

\```yaml
# 檔案: config/samples/{name}.yaml
apiVersion: {group}/{version}
kind: {Kind}
metadata:
  name: example
spec:
  # real sample from the repo
\```
```

## VitePress Configuration Pattern

When adding a new project, add these 3 things to `config.js`:

```javascript
// 1. Sidebar array (before export default)
const newProjectSidebar = [
  {
    text: '📖 {Project} 總覽',
    items: [
      { text: '專案簡介', link: '/{project-slug}/' },
      { text: '系統架構', link: '/{project-slug}/architecture' },
      { text: '核心功能分析', link: '/{project-slug}/core-features' },
      { text: '控制器與 API', link: '/{project-slug}/controllers-api' },
      { text: '外部整合', link: '/{project-slug}/integration' },
    ]
  },
]

// 2. Nav dropdown entry
{ text: '{Project Name}', link: '/{project-slug}/' },

// 3. Sidebar route mapping
'/{project-slug}/': newProjectSidebar,
```
